# PWKM Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies (standard library only for basic scripts)
# No external dependencies required for task_manager.py and date_utils.py

# Optional: For enhanced functionality
# requests>=2.28.0      # If adding API integrations
# python-dotenv>=1.0.0  # If using environment variables
